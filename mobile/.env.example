# Copy to .env. Use your machine's LAN IP (not localhost) when testing on a
# physical device with Expo Go, since "localhost" on the phone means the phone.
EXPO_PUBLIC_API_URL=http://192.168.1.50:4000
EXPO_PUBLIC_WS_URL=ws://192.168.1.50:4001
